mixin productCard(product = {})
  .product
    .product__text
      h2= product.name
      p= product.type
      p= product.model
      p= product.power
      p= product.base
    .product__image
      image
